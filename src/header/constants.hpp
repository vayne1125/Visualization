#pragma once
#define PI acos(-1)
enum METHODS
{
    ISO_SURFACE, SLICE_METHOD, RAY_CASTING, STREAMLINE, SAMMON_MAPPING
};

enum MODE
{
    AFTER_CALC, BEFORE_CALC
};

enum PROJECTION_METHODS
{
    PERSPECTIVE, ORTHO
};

